// DU1matrix.hpp
// Jaroslav Jindrak NPRG051 2014/2015
#include "du1matrix.hpp"

/**
 * Since all major method use template arguments, I have decided to leave all
 * definitions in the header file, the only ones that don't are default
 * constructors, size() style methods and the destructor of the matrix class,
 * which are all quite small (1-4 lines at most).
 */
